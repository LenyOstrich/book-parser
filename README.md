# 📘 Book Parser

## Описание проекта

**Book Parser** — это инструмент для сбора данных о книгах с сайта [Books to Scrape](https://books.toscrape.com).  
Он позволяет:
- получать данные о конкретной книге;
- собирать сведения обо всех книгах на сайте;
- по желанию сохранять результаты в файл;
- запускать процесс парсинга автоматически по расписанию.

---

## Технологии

Проект реализован на **Python 3.13** с использованием следующих библиотек и инструментов:

- `requests` — для выполнения HTTP-запросов;  
- `beautifulsoup4` — для парсинга HTML-страниц;  
- `schedule` — для планирования регулярного запуска скрипта;  
- `concurrent.futures` — для многопоточной обработки;  
- `pytest` — для тестирования;  
- `ruff` — для статического анализа и форматирования кода.

---

## Структура проекта

```
  books_scraper/
  ├── artifacts/
  │   └── books_data.txt
  ├── notebooks/
  │   └── HW_03_python_ds_2025.ipynb
  ├── tests/
  │   └── test_scraper.py
  │   └── __init__.py
  ├── .gitignore
  ├── README.md
  ├── requirements.txt
  ├── ruff.toml
  └── scraper.py
  ```
